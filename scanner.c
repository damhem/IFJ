
#include "scanner.h"

Token getNextToken(stack *indent_stack) {
  bool run = true;
  bool line_flag = true;

  while(run) {
    switch (state) {
      case state_start:
        //todo
        break;
    }
  }

}
